/** Badge that says whether the advert is still online at the portal. */
export const availability = {
  available: "available",
  gone: "no longer available",
  error: "cannot be checked",
  /** Tooltip; `{detail}` is the portal's own message and is appended separately. */
  checked: "Checked {age} ago",
};
